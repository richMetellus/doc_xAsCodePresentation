Using rST syntax, one can create a **bold text**, 
*italic text* and some ``inline code``.
One can do more powerful thing like code block 
with syntax highlighting as follow

.. code:: C
   
   int test(void);
